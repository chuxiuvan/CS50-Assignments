Mario Code for CS50 Week 1

#include <cs50.h>
#include <stdio.h>

// prototype function for print_row?
void print_row(int space, int bricks);

int main(void)
{ // Prompt user for pyramid height - input
int n;
    do
    {
    n = get_int("Height: ");
    }
    while (n < 1 || n > 8);
// print pyramid - output
for (int i = 1; i <= n; i++)
    {
// print row of bricks
    print_row(n - i, i);
    }
}
void print_row(int space, int bricks)
{
 for (int j = 1; j <= space; j = j + 1)
 {
    printf(" ");
 }
 for (int i = 1; i <= bricks; i = i + 1)
 {
    printf("#");